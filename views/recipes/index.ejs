<%- include('partials/header',{
    title: 'index page'
    }) %>
<%-include('./partials/navbar.ejs')%>

<h1>Cocktail Feed</h1>
<div class="recipe-feed">
        <ul>
            <% for(let i = 0; i < recipes.length; i++) { %>
                <li class='image'> <a href="/recipes/<%= recipes[i]._id%>"><img src="<%= recipes[i].photo %>"></a>
                </li> 
            <% } %>
        </ul>

</div>
    
</body>
</html>